# Gatefall Profile 1.35 — Ascension Eligibility and the Capability Ladders

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Status:** Approved by the owner on repository date 2026-07-29, including the Task 2 rung ruling. Cleared for execution.

**Goal:** Close the last hole in the Rank Dominance Law. A skill on the modifier-step / stated-capability axis whose target Rank has no authored category currently gains **nothing** from ascension, while Sections 7.2 and 7.5 promise three separate times that every skill gains. Make the promise true by (a) refusing to offer an ascension that would grant nothing, and (b) authoring the three reachable rungs that are missing.

**Architecture:** A rules-text correction plus three authored category rungs, confined to one world's rule profile and its adoption bookkeeping. It changes **no stored value** — no counter, Stat, pool, magnitude, cost, item, or resolved outcome — so it classifies as an **additive correction advance requiring no recomputation**.

**Tech Stack:** Markdown canonical records; PowerShell 5.1 validators (`tools/*.ps1`); git on `main`.

---

## Why this is urgent rather than routine

**Flash Step stands at `D-Rank ★★★★☆ Expert`** — one mastery level below Master. Mastery advances settle at dangerous-scene close, so the next real fight can take it to Master, at which point Section 7.5 becomes obliged to surface a breakthrough offer at the following readiness gate.

System Rank is **D**, so the ceiling (Section 7.5) is **C**. A C-Rank Flash Step would gain:

| What Rank grants a modifier-step skill | What Flash Step gets |
|---|---|
| Whole-scene duration at the first Rank above native | **Nothing** — the skill is instantaneous ("once"), there is no duration to saturate |
| One further affected subject per Rank | **Nothing** — it has no scope count; Section 7.2's *Inert until scope exists* rule applies |
| One authored category grant per Rank | **Nothing — no category is authored for Flash Step at any Rank** |

So the offer would cost **two mastery levels** (Master → Adept) and return a Rank label with no capability behind it, while announcing itself under a rule that says the trade "is always a net gain."

**This cannot be repaired after the fact.** Rules 13.2 and this profile's own migration discipline forbid retconning promoted canon, and an accepted ascension has already spent the mastery. The guard must land *before* the offer, or not at all.

**Sprint has the same shape** (native E-Rank, `+1 modifier step ... for one exchange`, no scope count, no authored category) but is not imminent.

**Six of the eight starting skills are unaffected** and need no change: Mend, Mana Bolt, Rupture and Twin Fang are damage/healing; Stone Skin and Bulwark are reduction; Dagger Mastery is a passive multiplier. The Law is a strict numeric comparison on all of those and is already true. The five Section 7.3 scope skills have authored ladders. The gap is exactly **Sprint and Flash Step**.

## The precedent this follows

Section 7.2 already solves this problem once, for reduction skills at the 90% cap:

> At and above the cap, each further Rank step instead grants **one further exchange of duration**, so ascending a capped reduction skill is still never worth nothing.

The profile already holds the principle that *a Rank step must never grant nothing*. It simply was not applied to the capability axis. Note that the reduction fallback works because a reduction skill is sustained and has duration to gain — Flash Step is instantaneous, so no generic fallback exists for it. That is precisely why the category has to be authored rather than substituted.

---

## Global Constraints

- **Profile version:** 1.34 → **1.35**. Engine Compatibility `0.2.0; Data Model 0.1.5` is **unchanged**.
- **Repository date for this adoption:** `2026-07-29`.
- **Next free Event id:** `EVT-000190` (registry high-water is `EVT-000189`).
- **Chronicle canonical record:** `REC-000079`. No new `REC-` is allocated.
- **Bearer entity:** `ENT-000125` (Alexander Pendragon).
- **No stored value may change.** No counter, Stat, pool, magnitude, cost, price, roll, item, or resolved outcome. Every skill keeps its current Rank and mastery level. Sprint and Flash Step both stand at their **native** Rank today, so every authored rung is prospective and nothing is retroactive.
- **No resolved roll is reopened and no fictional time is consumed.**
- **No ascension offer is issued, withdrawn, or re-adjudicated retroactively.** No offer is currently outstanding.
- **Immutable checkpoints are never retrofitted.** `campaigns/gatefall_pendragon_001/saves/` stays byte-unchanged.
- **Commits go straight to `main`, no `Co-Authored-By` trailer** (project convention).
- **Never state a mechanic without its Section number** (`.agents/skills/rules/SKILL.md`).
- Validators run as `powershell -ExecutionPolicy Bypass -File tools/<name>.ps1` — **forward slashes**; a backslash path is mangled when invoked from bash.
- Files are **LF**, never CRLF. This repo's `.gitattributes` mandates it and mixed endings have broken contract anchors before.
- `git commit` emits `failed to delete '.git/worktrees/...': Permission denied`. Pre-existing OneDrive locks, **not** failure. Verify with `git log --oneline -1`.

---

## File Structure

| File | Responsibility |
|---|---|
| `worlds/gatefall/206_WORLD_RULE_PROFILE.md` | §7.2 eligibility rule; §7.3 capability ladders; §7.5 offer binding and the two ordering claims; the version header |
| `system/WORLDS_AND_CAMPAIGNS.md` | World row version + the live readiness-state paragraph |
| `campaigns/gatefall_pendragon_001/090_CAMPAIGN_STARTUP.md` | `world_rule_profile` binding |
| `campaigns/gatefall_pendragon_001/100_CHARACTER_SHEET.md` | `profile_version` and `provenance.source` **only** |
| `campaigns/gatefall_pendragon_001/160_CAMPAIGN_CHRONICLE.md` | Adoption Event `EVT-000190` + subjects list + `provenance.source` |
| `campaigns/gatefall_pendragon_001/170_CHANGELOG.md` | Campaign-facing entry + `provenance.source` |
| `campaigns/gatefall_pendragon_001/180_CURRENT_STATE.md` | Profile line + checkpoint migration-chain text + `provenance.source` |
| `system/ID_REGISTRY.md` | `EVT-000190` allocation + high-water bump |
| `engine/030_ENGINE_CHANGELOG.md` | World-authoring entry (Decisions 062/069 — consumes **no** engine decision number) |
| `worlds/gatefall/README.md` | Version references |
| `tools/test_gatefall_economy_contract.ps1`, `tools/test_gatefall_quest_contract.ps1`, `tools/test_gatefall_recovery_contract.ps1` | Hardcoded `1.34` pins → `1.35` |
| `tools/validate_repository.ps1`, `tools/test_progression_audit_contract.ps1` | Ascension-eligibility validation + its contract test |

**Two lessons carried forward from Phase 1, in scope from the start:**

1. The three `test_gatefall_*` contracts pin the profile version in hardcoded assertions. Profile 1.31's plan omitted them and three validators went red two tasks later.
2. The four canonical ledgers carry a header `provenance.source` that every adoption must advance. Profile 1.31's plan never mentioned it and all four were left stranded on the prior Event.

---

## Task 1: §7.2 — the ascension-eligibility rule

**Files:** Modify `worlds/gatefall/206_WORLD_RULE_PROFILE.md` (§7.2, immediately after the **Stat Passive exemption** paragraph, ~line 978)

**Interfaces:** Produces the term **"ascension-eligible"**, consumed by Tasks 3 and 5.

- [ ] **Step 1: Confirm the anchor**

```bash
grep -n "Stat Passive exemption" worlds/gatefall/206_WORLD_RULE_PROFILE.md
```

Expected: exactly one hit, in §7.2. If not, re-derive the insertion point before editing.

- [ ] **Step 2: Insert the rule after that paragraph**

```markdown
**Ascension eligibility.** A skill is **ascension-eligible** at a target Rank only where that Rank's grant is authored and non-empty for it. On the two quantitative axes the grant is always authored: the reduction and passive-multiplier scales above are closed-form and every Rank has a value. On the **scope and capability** axis the grant is a category, and a category exists only where Section 7.3 authors one. Where a target Rank authors no category for the skill, and its whole-scene and further-subject grants are both inert — the first because the skill's authored effect has no duration to saturate, the second under *Inert until scope exists* above — that Rank would cost mastery levels and return nothing. **The skill is then not ascension-eligible, and Section 7.5 withholds the offer rather than making it.**

Withholding costs the Bearer nothing: the skill keeps its Rank, its mastery level, and its full effect, and becomes eligible the moment the rung is authored under Section 20.3. It is a deferral, not a denial — and it is the only order of operations this profile permits, because an ascension settles by consuming mastery and Rules Section 13.2 forbids reopening what a settlement has spent.

**A withheld offer is recorded, never silent.** At the settlement or promotion barrier that would have surfaced it, the Runtime states that the skill reached ascension-readiness and that no offer was made because the target Rank is unauthored, naming the skill and the Rank. An unauthored rung is a gap in this profile, and a gap that goes unstated is the defect that Section 8.4.6's telemetry exists to prevent elsewhere.
```

- [ ] **Step 3: Make the Law's universality claim explicit rather than merely asserted**

The blockquote at ~line 948 stays exactly as written — the eligibility rule makes it true by construction rather than by weakening. Append one sentence to the paragraph beginning `**Both contribute to the count, and the count is not what carries the law.**`:

```markdown
Where no category is authored at a target Rank the skill is not ascension-eligible at all (above), so no reachable state exists in which a skill one Rank higher fails to do something the lower-Rank Master cannot — the law holds for every skill in this profile because the profile declines to create the state that would break it.
```

- [ ] **Step 4: Validate and stop**

```bash
powershell -ExecutionPolicy Bypass -File tools/validate_repository.ps1
```

Expected exit 0. Rule prose only; no structure or counter arithmetic changes.

- [ ] **Step 5: Commit**

```
Author the ascension-eligibility rule for unauthored Ranks

Section 7.2 grants a modifier-step skill whole-scene duration, one further
subject, and one authored category per Rank. On a skill with no duration to
saturate and no scope count -- Sprint and Flash Step -- the first two are
inert and the third does not exist, so ascension would spend mastery levels
and return a Rank label with nothing behind it, under a law promising the
trade is always a gain.

Such a skill is now not ascension-eligible and the offer is withheld rather
than made. Withholding costs nothing and reverses the moment the rung is
authored; an accepted ascension could not be reversed at all.
```

---

## Task 2: §7.3 — author the capability-axis ladders

**Files:** Modify `worlds/gatefall/206_WORLD_RULE_PROFILE.md` (§7.3, immediately after the *Scope-axis category ladders* table's closing sentence, ~line 1069)

**Interfaces:** Consumes "ascension-eligible" from Task 1. Produces the rungs that make Sprint and Flash Step eligible.

> **Owner ruling (2026-07-29).** The three rung effects below are settled. Sprint takes **broken ground** at D and **vertical** at C as drafted. Flash Step takes **through the barrier** at C — the owner chose this over the drafted *passenger* rung after both were put with their trade-offs.
>
> The controller had flagged one risk against the barrier rung: the campaign's live threads lean on locked doors as real obstacles — the painted-over storefront's locked side door (`ENT-000164`) and the fenced, biometric-keypad warehouse (`ENT-000166`). The rung is therefore authored with two bounds that contain that risk without weakening it: it crosses **constructed** barriers only, and it requires a **read of the far side** or resolves as an uncertain action. It explicitly cannot cross a Gate boundary, a Section 9.6 seal, or a Section 17 instance wall — those are the edges of a space, not obstacles within one, and a Rank rung must not quietly repeal the sealed-instance rules.

- [ ] **Step 1: Confirm the anchor**

```bash
grep -n "Every skill in this table is at its native E-Rank today" worlds/gatefall/206_WORLD_RULE_PROFILE.md
```

Expected: exactly one hit. Insert after that line.

- [ ] **Step 2: Append the capability ladder**

A separate table is correct here rather than extra rows on the scope table: these two skills have no scope count, their native Ranks differ from each other, and the scope table's E/D/C column headings assume a native E-Rank that Flash Step does not have.

```markdown
**Capability-axis category ladders.** A modifier-step skill whose authored effect carries no scope count gains no subject from Rank (*Inert until scope exists*, Section 7.2) and, where its effect is instantaneous, no duration either. Rank reaches such a skill through the category alone, so the category must be authored for the skill to be ascension-eligible at all. Columns are stated relative to each skill's own native Rank, which differs between them.

| Skill | Native | First Rank above native | Second Rank above native |
|---|---|---|---|
| **Sprint** *(native E-Rank)* | +1 modifier step on movement, pursuit, and distance-closing actions | **Broken ground** *(D)* — the step holds across footing that would otherwise demand its own check: rubble, ice, slick or shifting surfaces, moving debris | **Vertical** *(C)* — vertical and overhanging surfaces count as ground while the effect holds; he crosses a wall or a girder as though it were floor |
| **Flash Step** *(native D-Rank)* | Close or break line-of-sight distance instantly once, granting +1 modifier step to the follow-up action | **Through the barrier** *(C)* — the step crosses a closed **constructed** barrier rather than only open distance: a door, shutter, partition, hatch, or fence he could pass if it stood open. He must already hold a read of the far side — a prior look, a resolved Keen Sense, or a Pattern relayed by an ally — or the step is blind and resolves as an uncertain action under Rules Section 4. It never crosses a Gate boundary, a Section 9.6 seal, or a Section 17 instance wall: those are the edges of a space, not obstacles within one | *Authored under Section 20.3 when B-Rank becomes reachable* |

Both skills stand at their **native** Rank today — Sprint at E, Flash Step at D — so every entry beyond the native column is prospective and changes nothing currently resolved. Flash Step's B rung is deliberately unauthored: the Section 7.5 ceiling is System Rank + 1, so B is unreachable at System Rank D, and Section 20.3 authors it at the checkpoint where it first becomes reachable. Until then Flash Step is ascension-eligible to C and not beyond.
```

- [ ] **Step 3: Confirm both skills stand at native Rank — nothing retroactive**

```bash
grep -on "\"Sprint \[.-Rank\]\|\"Flash Step \[.-Rank\]" campaigns/gatefall_pendragon_001/100_CHARACTER_SHEET.md
```

Expected: `Sprint [E-Rank]` and `Flash Step [D-Rank]`, matching the §7.3 Starting Skill Table's native Ranks. If either has ascended, **stop** — a live category grant would exist and the migration text in Task 4 must be rewritten to say so.

- [ ] **Step 4: Validate, then commit**

```bash
powershell -ExecutionPolicy Bypass -File tools/validate_repository.ps1
```

```
Author the capability-axis category ladders for Sprint and Flash Step

Section 7.2 spends every Rank above native on duration, subjects, and an
authored category. Sprint and Flash Step have no scope count, and Flash Step
is instantaneous, so the category was the only one of the three that could
reach them -- and it was never written.

Sprint gains broken ground at D and vertical movement at C; Flash Step gains
a barrier crossing at C. Flash Step's B rung stays unauthored under Section 20.3
because the Section 7.5 ceiling puts it out of reach at System Rank D. Both
skills stand at native Rank, so nothing is retroactive.
```

---

## Task 3: §7.5 — bind the offer to eligibility

**Files:** Modify `worlds/gatefall/206_WORLD_RULE_PROFILE.md` (§7.5, three edits)

**Interfaces:** Consumes "ascension-eligible" from Task 1.

- [ ] **Step 1: Qualify the net-gain claim**

Old (within the `**Rune ascension is offered too.**` paragraph):

```markdown
Under the Section 7.2 Rank Dominance Law the trade is always a net gain
```

New:

```markdown
Under the Section 7.2 Rank Dominance Law every trade this profile offers is a net gain — a Rank that would grant nothing is not offered at all, because Section 7.2's eligibility rule withholds it
```

- [ ] **Step 2: Qualify the ordering claim**

Old:

```markdown
The full ordering, which holds for every skill in this profile, is therefore:
```

New:

```markdown
The full ordering, which holds for every skill this profile makes ascension-eligible, is therefore:
```

- [ ] **Step 3: Bind the breakthrough settlement**

Old:

```markdown
**Breakthrough settlement.** Breakthrough is **offered, never imposed.** When a skill's mastery advances to Master and its Rank stands below the ceiling, it becomes **ascension-ready** and the Runtime surfaces the offer
```

New:

```markdown
**Breakthrough settlement.** Breakthrough is **offered, never imposed.** When a skill's mastery advances to Master, its Rank stands below the ceiling, and it is **ascension-eligible** at the next Rank under Section 7.2, it becomes **ascension-ready** and the Runtime surfaces the offer
```

- [ ] **Step 4: Extend the misstated-offer rule**

Old:

```markdown
An offer that omits a category grant or shows a count below the Section 7.2 ratchet floor is a misstated offer and is corrected like any misstated rule.
```

New:

```markdown
An offer that omits a category grant or shows a count below the Section 7.2 ratchet floor is a misstated offer and is corrected like any misstated rule. An offer surfaced for a skill that is **not ascension-eligible** is a misstated offer of the same kind: it is withdrawn, the withholding is recorded under Section 7.2, and any mastery it consumed is restored, since the offer should never have been presentable.
```

- [ ] **Step 5: Validate, then commit**

```
Withhold ascension offers for skills no Rank would improve

Section 7.5 asserted three times that every skill gains from ascension --
in the net-gain sentence, in the Rank N ordering, and by making every
Master-level skill below the ceiling ascension-ready. All three were false
for a skill with no authored category at the target Rank.

The offer now requires Section 7.2 eligibility, and an offer surfaced
without it is a misstated offer that restores the mastery it consumed.
```

---

## Task 4: Version the profile 1.34 → 1.35, and every pin

**Files:** `worlds/gatefall/206_WORLD_RULE_PROFILE.md` (header), `worlds/gatefall/README.md`, `system/WORLDS_AND_CAMPAIGNS.md`, `campaigns/gatefall_pendragon_001/090_CAMPAIGN_STARTUP.md`, `campaigns/gatefall_pendragon_001/100_CHARACTER_SHEET.md`, and all three `tools/test_gatefall_*_contract.ps1`

- [ ] **Step 1: Enumerate every live pin before editing** — sweep, do not fix one at a time

```bash
grep -rn "1\.34" --include=*.md --include=*.ps1 . | grep -v "saves/\|\.tmp/\|\.superpowers/\|docs/superpowers\|170_CHANGELOG\|030_ENGINE_CHANGELOG\|160_CAMPAIGN_CHRONICLE\|ID_REGISTRY"
```

Expected **23** live pins across 9 files, in four classes. Advance every one to `1.35`, in both the regex **and** the message string wherever a test pins it:

| Class | Locations |
|---|---|
| **Version identity** | `206_WORLD_RULE_PROFILE.md:1` (title), `:6` (`**Profile Version:**`), `:9` (`frozen at version`) |
| **Migration-treatment string** | `206_WORLD_RULE_PROFILE.md:11` (`**Required 1.33 → 1.34 migration.**` → `1.34 → 1.35`), and the two contract tests that pin it: `test_gatefall_economy_contract.ps1:44` and `test_gatefall_quest_contract.ps1:248`, both matching `Required 1\.33.+1\.34 migration` |
| **Live bindings** | `090_CAMPAIGN_STARTUP.md:12`, `100_CHARACTER_SHEET.md:69`, `WORLDS_AND_CAMPAIGNS.md:29` (world row) and `:59` (live readiness paragraph), `README.md:4`, `:5`, `:48` |
| **Contract pins** | economy `:41`, `:55`, `:58`; quest `:35`, `:47`, `:54`, `:58`; recovery `:70` |

**The migration-treatment class is the one that bites.** It is not a version number in isolation — it names a *pair* of versions, so it needs both halves advanced, and two separate contract tests assert it. This class is easy to miss precisely because the naive sweep for the outgoing version number does find it but it does not look like the others.

`180_CURRENT_STATE.md:28` and `:33` are also live pins; they belong to **Task 6 Step 5** and are deliberately not touched here. Expect the sweep to surface them and leave them.

Leave **historical** references at 1.34: `180_CURRENT_STATE.md:93` (checkpoint capture narration), `test_gatefall_quest_contract.ps1:246` (section comment naming 1.34's feature), `ID_REGISTRY` rows describing earlier Events, and the retained header paragraphs for prior versions. `test_gatefall_recovery_contract.ps1:127` reads "Profile 1.32 retained through Profile 1.34" — a comment describing what is currently retained, so advance it to 1.35 for accuracy even though nothing asserts on it.

- [ ] **Step 2: Bump the title and version field**

```
# Gatefall — World Rule Profile 1.34   →   # Gatefall — World Rule Profile 1.35
**Profile Version:** 1.34              →   **Profile Version:** 1.35
```

- [ ] **Step 3: Demote the 1.34 header paragraph and add 1.35's**

The header retains every prior version's paragraph, rewritten from "is" to "remains the" when superseded. Open the new `**Compatibility Status:**` with:

```markdown
**Compatibility Status:** frozen at version 1.35 (Rules Section 14.6, Decision 074), declared on repository date 2026-07-29. Version 1.35 is an **additive correction advance** over frozen 1.34, closing the last hole in the Section 7.2 Rank Dominance Law. Section 7.2 grants a modifier-step or stated-capability skill three things per Rank above native — whole-scene duration at the first step, one further affected subject, and one authored category — but on a skill whose effect is instantaneous and carries no scope count the first two are inert and the third existed for no skill outside Section 7.3's scope table. **Sprint and Flash Step could therefore ascend for nothing**, spending mastery levels under a law promising the trade is always a gain, and Section 7.5 asserted that promise in three separate places. A skill is now **ascension-eligible** only where the target Rank's grant is authored and non-empty; where it is not, Section 7.5 withholds the offer and records the withholding rather than presenting a Rank step with nothing behind it. Section 7.3 additionally authors the reachable capability rungs — Sprint's **broken ground** at D and **vertical** at C, Flash Step's **through the barrier** at C — so both skills become eligible rather than merely protected. **No stored field, magnitude, cost, counter, probability, owned item, completed transaction, or resolved outcome changes.** Data Model 0.1.5 remains unchanged.

**1.34 → 1.35 compatibility treatment.** Preserve every stored value and all resolved canon. Adoption requires no recomputation and no state migration: every skill's Rank, mastery level, counters, and rendered effect are identical before and after. **Sprint stands at native E-Rank and Flash Step at native D-Rank**, so no capability rung is live and none is retroactive — the ladders are prospective in full. **No ascension offer is reopened, withdrawn, or issued retroactively**, and none is outstanding at adoption. Flash Step stands at Expert, one mastery level below the Master that would have made it ascension-ready; this adoption reaches it before that settlement rather than after, which is the only order in which it could be reached at all. Immutable Profile 1.34-and-earlier checkpoints remain byte-unchanged and run the applicable compatibility chain through 1.35 at readiness.

Version 1.34 remains the ...
```

> **Implementer note:** the text following `Version 1.34 remains the` is the existing 1.34 paragraph with its opening rewritten. Do not delete it.

- [ ] **Step 4: Verify no live pin is stranded**

Re-run Step 1's sweep. Every remaining `1.34` must be historical. Then:

```bash
powershell -ExecutionPolicy Bypass -File tools/validate_repository.ps1
powershell -ExecutionPolicy Bypass -File tools/test_checkpoint_contract.ps1
powershell -ExecutionPolicy Bypass -File tools/test_gatefall_economy_contract.ps1
powershell -ExecutionPolicy Bypass -File tools/test_gatefall_quest_contract.ps1
powershell -ExecutionPolicy Bypass -File tools/test_gatefall_recovery_contract.ps1
```

All five exit 0. **Do not proceed on a red repository** — a stale pin here is exactly what broke three validators during Profile 1.31.

- [ ] **Step 5: Commit**

```
Version Gatefall Profile 1.34 to 1.35

Additive correction advance. No stored field, magnitude, cost, counter,
probability, owned item, completed transaction, or resolved outcome
changes; Data Model 0.1.5 is unchanged. Sprint stands at native E-Rank and
Flash Step at native D-Rank, so no capability rung is live.
```

---

## Task 5: Enforce eligibility, test-first

> **Tasks 5 and 6 form one commit unit.** Task 5 leaves the repository red — the validation exists before the adoption record it reports against. Do not commit at the end of Task 5.

**Files:** `tools/test_progression_audit_contract.ps1`, `tools/validate_repository.ps1`

The rule to enforce: **a skill's live Rank may not exceed the highest Rank authored for it.** That is the machine-checkable half of eligibility — it cannot catch an offer that was never surfaced, but it does catch the state such an offer would produce.

- [ ] **Step 1: Write the failing assertion first**

In `tools/test_progression_audit_contract.ps1`, before the cleanup block, append an assertion that mutates the live Flash Step entry to `[B-Rank]` — one Rank above its authored ceiling — and asserts the validator rejects it with a message matching `*exceeds its authored category ladder*`, then restores the entry and asserts a clean pass. Follow the existing `Get-CounterLine` / `Replace-Once` / `Invoke-Validation` helper pattern already in the file; derive the anchor from live state rather than hardcoding it.

- [ ] **Step 2: Run it and watch it fail for the right reason**

```bash
powershell -ExecutionPolicy Bypass -File tools/test_progression_audit_contract.ps1
```

Expected: FAIL because the validation does not exist yet — a B-Rank Flash Step is currently accepted. If it fails on a drifted anchor instead, re-derive the anchor against the current file rather than weakening the assertion.

- [ ] **Step 3: Add the validation**

In `tools/validate_repository.ps1`, beside the existing Stat Passive block, add a check keyed on the two capability skills and the five scope skills: each skill's rendered Rank must not exceed the highest Rank its Section 7.3 ladder authors. Model the structure on the existing `$statPassives` table — a small declarative list of skill name and highest authored Rank, walked once per Bearer entity.

- [ ] **Step 4: Re-run — the contract test now passes on the mutation and the live repository stays green**

```bash
powershell -ExecutionPolicy Bypass -File tools/test_progression_audit_contract.ps1
powershell -ExecutionPolicy Bypass -File tools/validate_repository.ps1
```

Both exit 0. Live state is unaffected: Sprint at E and Flash Step at D are both within their authored ladders.

---

## Task 6: Record the adoption

**Files:** `system/ID_REGISTRY.md`, `campaigns/gatefall_pendragon_001/160_CAMPAIGN_CHRONICLE.md`, `campaigns/gatefall_pendragon_001/170_CHANGELOG.md`, `campaigns/gatefall_pendragon_001/180_CURRENT_STATE.md`, `engine/030_ENGINE_CHANGELOG.md`

- [ ] **Step 1: Allocate `EVT-000190`** — bump the high-water at `system/ID_REGISTRY.md` line ~29, then add after the `EVT-000189` row:

```
| EVT-000190 | Event | Gatefall: Pendragon: adoption of World Rule Profile 1.35 - a skill is ascension-eligible only where the target Rank's grant is authored and non-empty, Section 7.5 withholds and records the offer where it is not, and Section 7.3 authors Sprint's broken-ground and vertical rungs and Flash Step's through-the-barrier rung, bounded to constructed barriers and barred from Gate boundaries, Section 9.6 seals and Section 17 instance walls |
```

- [ ] **Step 2: Append the adoption Event** — add `  - EVT-000190` to the `REC-000079` subjects list, then the Event at end of file with `kind: profile-adoption`, `importance: minor`, `counter_deltas: []`, participants `ENT-000125`, and the anchor unchanged at Owen Callahan's kitchen, ~15:10 on 2026-08-06. The description must state: the defect (three universality claims false for two skills); the repair (eligibility plus the three authored rungs); that **Flash Step was one mastery level from the offer** and this adoption precedes rather than follows that settlement; and that nothing stored changed.

- [ ] **Step 3: Campaign changelog entry** in `170_CHANGELOG.md` under `## Changes`, following the established four-bullet shape: what was wrong, what changed in the profile, what changed in live state (**nothing but `profile_version` and provenance**), repository allocation, validators.

- [ ] **Step 4: Engine changelog entry** in `engine/030_ENGINE_CHANGELOG.md` — world authoring, Decisions 062/069, consuming no engine decision number.

- [ ] **Step 5: `180_CURRENT_STATE.md`** — advance the profile line to **1.35** with a short Section-cited description, demote 1.34 into the existing "remains operative beneath it" chain, and update the checkpoint migration-chain text to name the additive 1.34 → 1.35 step at readiness. **Leave checkpoint capture versions at their captured value** — those describe immutable captures.

- [ ] **Step 6: Advance `provenance.source` to `EVT-000190`** in the header YAML block (~line 16) of all four canonical ledgers: `100_CHARACTER_SHEET.md`, `160_CAMPAIGN_CHRONICLE.md`, `170_CHANGELOG.md`, `180_CURRENT_STATE.md`. Change **only** the `source:` line; `game_date` and `real_date` are already correct. Do not touch the per-Event `provenance:` blocks further down the chronicle — those are historical.

- [ ] **Step 7: Run every validator**

```bash
powershell -ExecutionPolicy Bypass -File tools/validate_repository.ps1
powershell -ExecutionPolicy Bypass -File tools/test_checkpoint_contract.ps1
powershell -ExecutionPolicy Bypass -File tools/test_progression_audit_contract.ps1
powershell -ExecutionPolicy Bypass -File tools/test_gatefall_economy_contract.ps1
powershell -ExecutionPolicy Bypass -File tools/test_gatefall_quest_contract.ps1
powershell -ExecutionPolicy Bypass -File tools/test_gatefall_recovery_contract.ps1
```

All six exit 0.

- [ ] **Step 8: Read back — confirm nothing moved**

```bash
git diff <plan-start-sha> -- campaigns/gatefall_pendragon_001/100_CHARACTER_SHEET.md
```

Expected: **only** `profile_version` and `provenance.source`. Any other line means a stored value moved — revert and investigate.

- [ ] **Step 9: Commit Tasks 5 and 6 together**

```
Adopt Gatefall Profile 1.35: ascension eligibility

Records the adoption as EVT-000190 and enforces the authored-ladder ceiling
in the repository validator with a contract test covering a skill whose Rank
exceeds it.

No fictional time consumed, no resolved roll reopened, no stored value
changed, no ascension offer issued or withdrawn. Sprint stands at native
E-Rank and Flash Step at native D-Rank; both are now ascension-eligible to
C, where before neither would have gained anything from the Rank it was
about to be offered.
```

---

## Verification

Phase complete when all hold. Run them and paste real output; do not assert from memory.

- [ ] All six validators exit 0, and the working tree is clean.
- [ ] `git diff <plan-start-sha> -- campaigns/gatefall_pendragon_001/saves/` prints nothing.
- [ ] The character-sheet delta is exactly `profile_version` and `provenance.source`.
- [ ] `grep -c "ascension-eligible" worlds/gatefall/206_WORLD_RULE_PROFILE.md` returns **≥ 5** (§7.2 rule ×3, §7.5 ×2).
- [ ] Sprint still renders `[E-Rank]` and Flash Step still renders `[D-Rank]` on the sheet.
- [ ] No live `1.34` pin remains; every survivor is historical.
- [ ] `provenance.source` is `EVT-000190` in all four ledgers.
- [ ] `git log <plan-start-sha>..HEAD --format=%B | grep -ci "co-authored-by"` returns `0`.

---

## Out of scope

- **Flash Step's B rung and Sprint's B rung and above** — authored under Section 20.3 when the Section 7.5 ceiling first makes them reachable. Authoring them now would be inventing content for a Rank that cannot be held.
- **B/A/S rungs for the five Section 7.3 scope skills** — same reason, unchanged by this plan.
- **Any change to the Bearer's Health, Mana, damage, Stats, or the Stat Passive ladders.**
- **Re-adjudicating any past ascension.** Rupture's E→D breakthrough stands as settled.
- **`worlds/gatefall/230_KNOWLEDGE_SUBJECTS.md:126`** — the stale `Rank-Sight` reference in a concealed-discovery `clue_condition`, found in the same audit. Unrelated to ascension; fix it separately so this adoption's diff stays confined to one mechanic.
- **Reikon, Asterra and Verra** — Gatefall only.
